<!-- Navigation -->
<nav class="navbar navbar-default navbar-fixed-top" role="navigation" id="navbar">
  <div class="container">
    <div class="navbar-header page-scroll">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand page-scroll" id="home-link" href="/">HieroGiffics</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li>
          <%= link_to "New Gif", new_gif_path, data: { toggle: "modal", target: "#main_modal", remote: true } %>
          </li>
          <li>
            <%= link_to "Log Out", logout_path, remote: true %>
          </li>
        <% else %>
          <li>
            <%= link_to "Log In", login_path, data: { toggle: "modal", target: "#main_modal", remote: true } %>
          </li>
          <li>
            <%= link_to "Sign Up", new_user_path, data: { toggle: "modal", target: "#main_modal", remote: true } %>
          </li>
        <% end %>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container -->
</nav>
